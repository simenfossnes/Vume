import initialState from '../../store/initialState';

export default function commentReducer(state = initialState.entities.comments, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}