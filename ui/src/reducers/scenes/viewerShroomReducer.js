import initialState from '../../store/initialState';

export default function viewerShroomReducer(state = initialState.scenes.viewerShroom, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}