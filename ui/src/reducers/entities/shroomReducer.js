import initialState from '../../store/initialState';

export default function shroomReducer(state = initialState.entities.shrooms, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}