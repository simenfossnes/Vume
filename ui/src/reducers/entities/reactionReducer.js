import initialState from '../../store/initialState';

export default function reactionReducer(state = initialState.entities.reactions, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}