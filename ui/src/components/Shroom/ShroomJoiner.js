import React from 'react';
import PropTypes from 'prop-types';
import './ShroomJoiner.css';

const ShroomJoiner = (props) => (
    <div>
        Shroom Creator
    </div>
);

ShroomJoiner.propTypes = {

};

export default ShroomJoiner;