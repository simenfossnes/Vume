import initialState from '../../store/initialState';

export default function homeReducer(state = initialState.scenes.home, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}