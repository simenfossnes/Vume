import initialState from '../../store/initialState';

export default function shroomCreatorReducer(state = initialState.scenes.shroomCreator, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}