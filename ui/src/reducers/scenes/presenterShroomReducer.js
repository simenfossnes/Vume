import initialState from '../../store/initialState';

export default function presenterShroomReducer(state = initialState.scenes.presenterShroom, action) {
    switch(action.type) {
        default:
            break;
    }
    return state;
}